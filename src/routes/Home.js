import {Widget, Inbox} from '../components'

const Home = () => {
    return (
        <main>
            <div style={{backgroundColor: "pink", width: "100%"}}>
                <Widget/>
            </div>
            <Inbox/>
        </main>
    )
}

export default Home
